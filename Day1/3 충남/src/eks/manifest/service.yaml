apiVersion: v1
kind: Service
metadata:
  name: wsc2026-book-svc
  namespace: wsc2026
  annotations:
    service.kubernetes.io/topology-mode: "Auto"
spec:
  selector:
    app: book
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080